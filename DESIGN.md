# 本地 AI 执行器设计说明

## 目标

这个项目实现一个最小可用的本地 AI 执行器，用来配合 `cursor2api` 转发后的 Claude 模型。

核心目标只有两个：

1. 让模型通过结构化 JSON 请求本地工具。
2. 让本地程序在安全边界内执行这些工具，并把结果继续喂回模型，形成循环。

这版不是完整 agent 框架，而是一个最小闭环：

- 调模型
- 模型返回工具调用 JSON
- 本地执行
- 回传结果
- 继续下一轮
- 直到 `done=true`


## 设计思路

### 1. 不依赖真实 tool calling，统一收敛成 JSON 协议

虽然接口请求里带了 `tools` 描述，但本地执行器不依赖服务端真的返回标准化 tool use block。

原因很简单：

- `cursor2api` 可能对不同上游模型做兼容层转发
- Claude 风格返回体和 OpenAI 风格返回体并不完全一致
- 最小版本优先保证可控和稳定

所以我这里选择了一个更硬的约束：

- 模型必须只输出一个 JSON 对象
- 如果要调用工具，输出：

```json
{"done": false, "tool": "read_file", "arguments": {"path": "a.txt"}}
```

- 如果任务结束，输出：

```json
{"done": true, "final_answer": "任务完成"}
```

这样做的好处是：

- 本地解析逻辑非常简单
- 兼容性更强
- 方便调试
- 失败面更小


### 2. 执行器拆成 3 个模块

#### `main.py`

负责外部入口和主循环：

- 接收用户任务
- 调用 cursor2api 接口
- 解析模型返回 JSON
- 执行工具
- 把工具结果再发回模型
- 循环直到结束

它解决的是“控制流”问题。


#### `tools.py`

负责本地工具实现和安全限制：

- `write_file(path, content)`
- `read_file(path)`
- `list_dir(path)`
- `run_shell(command)`

它解决的是“能力边界”问题。


#### `executor.py`

负责模型 JSON 协议的解释和调度：

- 校验模型是否返回合法 JSON
- 判断当前是不是结束态
- 分发到对应工具函数
- 统一包装工具执行结果

它解决的是“协议解释”问题。


## 为什么这样拆

这个拆法的重点是把三个变化频率不同的部分分开：

1. 模型接入方式会变
2. 工具集合会变
3. 执行协议也可能会变

如果都写在一个文件里，后面一旦你要扩工具、换模型、补多轮记忆，代码会很快缠在一起。

当前这个拆法虽然简单，但已经够支撑后续迭代：

- 想加工具，只动 `tools.py`
- 想换响应协议，只动 `executor.py`
- 想换接口地址、模型参数、消息组织方式，只动 `main.py`


## 主循环设计

整体流程如下：

1. 用户输入一个任务。
2. `main.py` 组装 system prompt 和第一轮 user message。
3. 请求 `cursor2api /v1/messages`。
4. 模型返回一个 JSON 对象。
5. `executor.py` 解析 JSON。
6. 如果 `done=true`，直接结束。
7. 如果 `done=false`，根据 `tool` 和 `arguments` 执行本地工具。
8. 把工具执行结果作为新的上下文发回模型。
9. 重复上述流程，直到完成或达到最大步数。

这个循环本质上就是一个非常小的 agent runtime。


## 安全设计

最小版里最重要的不是能力，而是边界。

### 1. 文件操作限制在 `./workspace`

所有路径都通过 `resolve_workspace_path()` 解析：

- 用户传的是相对路径
- 本地统一拼接到 `workspace/`
- 再做绝对路径归一化
- 如果目标路径逃逸出 `workspace/`，直接拒绝

这一步是必须的，因为模型会犯错，也可能被 prompt 注入影响。


### 2. Shell 只允许白名单命令

`run_shell(command)` 只允许：

- `ls`
- `node`
- `python`

并且只检查拆分后的第一个命令词。

也就是说，像下面这些会被拒绝：

- `rm -rf .`
- `bash test.sh`
- `cat /etc/passwd`

这样做的目的是先把攻击面压到最小。


### 3. Shell 工作目录固定在 `workspace`

即使是允许的命令，也只在 `workspace/` 下执行，而不是整个项目根目录。

这可以降低误操作影响范围。


### 4. 设置最大循环次数

`--max-steps` 默认是 10。

这用于防止模型陷入无穷循环，例如：

- 一直重复 `list_dir`
- 一直读同一个文件
- 工具失败后无法自我修正


## 为什么不用更复杂的方案

这版刻意没有做下面这些东西：

- 不做异步
- 不做并发工具调用
- 不做真正的流式输出
- 不做完整对话历史压缩
- 不做权限系统分级
- 不做 JSON schema 严格校验
- 不做 shell 沙箱隔离
- 不做标准 Anthropic tool_use block 解析

原因不是这些不重要，而是它们会明显提高复杂度，偏离“最小可用版本”的目标。

当前设计的重点是先把闭环跑通。


## 这版的局限

### 1. 过度依赖 prompt 约束

模型是“被要求”只输出 JSON，但不是协议层强制。

如果模型输出了多余解释文本，当前实现会直接报 JSON 解析错误。


### 2. `run_shell` 仍然只是轻量限制

虽然有白名单，但还不是真正的安全沙箱。

例如：

- `python script.py` 本身仍然可能做很多事
- `node` 同理

因此这版适合本地受控环境，不适合直接暴露给不可信输入。


### 3. 没有任务状态抽象

现在的状态直接堆在消息数组里，没有单独的 runtime state 对象。

后续如果你要加：

- 计划状态
- 观察历史
- 中间变量
- 成本统计

最好单独抽一层状态管理。


## 后续建议迭代

如果下一步继续做，我建议按这个顺序推进：

### 第一阶段：增强稳定性

1. 给模型输出加更严格的 JSON schema 校验
2. 对接口异常、超时、空响应做更清晰的错误处理
3. 给工具结果加统一错误码
4. 给每一步加日志落盘


### 第二阶段：增强安全性

1. `run_shell` 改成更细粒度的参数白名单
2. 拒绝重定向、管道、子进程链式调用
3. 增加执行超时和输出长度限制
4. 对 `python` / `node` 再增加脚本路径限制


### 第三阶段：增强 agent 能力

1. 增加 `mkdir`
2. 增加 `delete_file`
3. 增加 `append_file`
4. 增加多文件 diff 输出
5. 增加任务计划与总结


### 第四阶段：增强协议兼容

1. 兼容标准 Anthropic `tool_use`
2. 兼容 OpenAI function calling 风格
3. 把本地工具层抽象成统一 Tool Registry


## 总结

这个实现的核心思路是：

- 用最简单的 JSON 协议代替复杂 tool calling 适配
- 用主循环把“模型决策”和“本地执行”串起来
- 用 `workspace` 和命令白名单做最基础的安全边界
- 用模块拆分为后续扩展留出空间

如果把它放到 Claude Code 这一类系统的语境里，它相当于一个极简版 runtime：

- `main.py` 是 agent loop
- `executor.py` 是协议解释器
- `tools.py` 是本地能力层

这已经足够作为后续工程化版本的起点。
